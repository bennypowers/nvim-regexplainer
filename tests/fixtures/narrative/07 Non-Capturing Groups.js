/**
 * `hello`
 * non-capturing group:
 *   `world`
 */
/hello(?:world)/;

/**
 * `hello`
 * non-capturing group:
 *   `mudda`
 *   non-capturing group:
 *     `fadda`
 */
/hello(?:mudda(?:fadda))/;

